# codex

Personal working repository.

## Getting started

This repository was initialized as an empty Git project. Add your source files,
then commit and push as usual:

```bash
git add .
git commit -m "your message"
git push
```
